<form method="post" action="{{ path('produit_delete', {'idProduit': produit.idProduit},{'button_label': 'Delete'}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.idProduit) }}">
    <button class="badge badge-warning">Delete</button>
</form>
