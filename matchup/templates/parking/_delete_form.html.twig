<form method="post" action="{{ path('parking_delete', {'idParking': parking.idParking}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parking.idParking) }}">
    <button class="btn">Delete</button>
</form>
