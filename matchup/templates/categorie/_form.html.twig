{#{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}#}
{{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
<div  >

    <div class="form-group">
        {{ form_label(form.nomCategorie, "nom") }}

    </div>

    <div class="form-group">
        <input {{ form_widget(form.nomCategorie, {'attr':{'class':'form-control col-12 col-md-6 col-lg-6 '}}) }}
    </div>


        <button class="btn btn-success">{{ button_label|default('Ajouter') }}</button>
    </div>
</div>
</div>
</div>


</div>
</div>

{{ form_end(form) }}

