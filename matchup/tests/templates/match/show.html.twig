{% extends 'base.html.twig' %}

{% block title %}Match{% endblock %}

{% block body %}
    <h1>Match</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdMatch</th>
                <td>{{ match.idMatch }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ match.type }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ match.date }}</td>
            </tr>
            <tr>
                <th>Nbjoueurs</th>
                <td>{{ match.nbjoueurs }}</td>
            </tr>
            <tr>
                <th>NomTournoi</th>
                <td>{{ match.nomTournoi }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('match_index') }}">back to list</a>

    <a href="{{ path('match_edit', {'idMatch': match.idMatch}) }}">edit</a>

    {{ include('match/_delete_form.html.twig') }}
{% endblock %}
