{% extends 'base.html.twig' %}

{% block title %}Materiel index{% endblock %}

{% block body %}
    <h1>Materiel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdMateriel</th>
                <th>NomTerrain</th>
                <th>TypeMat</th>
                <th>QuantMat</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for materiel in materiels %}
            <tr>
                <td>{{ materiel.idMateriel }}</td>
                <td>{{ materiel.nomTerrain }}</td>
                <td>{{ materiel.typeMat }}</td>
                <td>{{ materiel.quantMat }}</td>
                <td>
                    <a href="{{ path('materiel_show', {'idMateriel': materiel.idMateriel}) }}">show</a>
                    <a href="{{ path('materiel_edit', {'idMateriel': materiel.idMateriel}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('materiel_new') }}">Create new</a>
{% endblock %}
