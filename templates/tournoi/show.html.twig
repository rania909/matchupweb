{% extends 'base.html.twig' %}

{% block title %}Tournoi{% endblock %}

{% block body %}
    <h1>Tournoi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdTournoi</th>
                <td>{{ tournoi.idTournoi }}</td>
            </tr>
            <tr>
                <th>NomTournoi</th>
                <td>{{ tournoi.nomTournoi }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ tournoi.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tournoi_index') }}">back to list</a>

    <a href="{{ path('tournoi_edit', {'idTournoi': tournoi.idTournoi}) }}">edit</a>

    {{ include('tournoi/_delete_form.html.twig') }}
{% endblock %}
