{% extends 'base.html.twig' %}

{% block title %}Resterrain{% endblock %}

{% block body %}
    <h1>Resterrain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdRester</th>
                <td>{{ resterrain.idRester }}</td>
            </tr>
            <tr>
                <th>DateRes</th>
                <td>{{ resterrain.dateRes }}</td>
            </tr>
            <tr>
                <th>NomTerrain</th>
                <td>{{ resterrain.nomTerrain }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resterrain_index') }}">back to list</a>

    <a href="{{ path('resterrain_edit', {'idRester': resterrain.idRester}) }}">edit</a>

    {{ include('resterrain/_delete_form.html.twig') }}
{% endblock %}
