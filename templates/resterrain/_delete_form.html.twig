<form method="post" action="{{ path('resterrain_delete', {'idRester': resterrain.idRester}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resterrain.idRester) }}">
    <button class="btn">Delete</button>
</form>
